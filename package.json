{
  "name": "date-gap",
  "version": "0.0.1",
  "description": "Find the amount of days, weeks, months, years between dates",
  "main": "date.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Chadwick",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-copy": "0.0.2",
    "gulp-rename": "^1.2.0",
    "gulp-uglify": "^1.1.0"
  }
}
